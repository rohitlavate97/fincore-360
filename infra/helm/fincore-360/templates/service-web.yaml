apiVersion: v1
kind: Service
metadata:
  name: {{ include "fincore.fullname" . }}-web
  labels:
    {{- include "fincore.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fincore.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
