apiVersion: v1
kind: Service
metadata:
  name: fincore-backend
  namespace: fincore
  labels:
    app.kubernetes.io/name: fincore-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: fincore-360
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: fincore-backend
